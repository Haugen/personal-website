---
export type Props = {
  name: string;
  title: string;
};

const { name, title } = Astro.props;
---

<div class="flex flex-col text-center my-8 lg:px-36">
  <quote class="text-2xl md:text-3xl lg:text-3xl mb-4 text-slate-800"
    ><slot />
  </quote>
  <div
    class="flex w-full flex-col lg:flex-row justify-center lg:gap-x-2 text-xl text-gray-500"
  >
    <p class="text-black">{name}</p>
    <span class="hidden lg:inline">–</span>
    <p>{title}</p>
  </div>
</div>
